version: "2"

networks:
  internal:
    driver: bridge

services:
  albank:
    container_name: "albank"
    image: "node:lts-alpine"
    working_dir: /app
    volumes:
      - "../../front:/app"
    command: "yarn serve"
    networks:
      - internal
    ports:
      - "3180:5000"
    labels:
      - "traefik.enable=true"
      - traefik.http.routers.frontalbank.rule=Host(`albank.alvitre.com.br`)
      - traefik.http.services.frontalbank.loadbalancer.server.port=5000